```python
Example:
def fact_iter(n):
    if n ==1:
        return 1
    else:
        return n*(n-1)
fact_iter(4)
```
# Key distcinctions i initially overlooked
1. All recursive calls have **Global** as their parent. 
2. A function's parent frame is fixed at definition time. 
